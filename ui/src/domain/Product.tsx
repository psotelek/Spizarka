export interface ProductType {
    name: string,
    category: string,
    measure: string
}

export interface ProductInfo {
    type: number,
    amount: number,
    note: string,
    name: string,
    expiry_date: string
}